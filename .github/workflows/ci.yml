name: CI

on:
  push:
    branches: [ release, develop ]
  pull_request:
    branches: [ release, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        php-version: [8.1, 8.2, 8.3]
    
    steps:
    - uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php-version }}
        tools: composer:v2
        coverage: xdebug

    - name: Cache Composer dependencies
      uses: actions/cache@v3
      with:
        path: /tmp/composer-cache
        key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}

    - name: Create test .env file
      working-directory: backend
      run: |
        ls -la .env*
        cp .env.test .env
        ls -la .env
        cat .env

    - name: Install dependencies
      working-directory: backend
      run: |
        composer install --no-progress --prefer-dist --optimize-autoloader

    - name: Setup test environment and database
      working-directory: backend
      run: |
        mkdir -p var/data var/log var/cache var/coverage
        chmod -R 755 var/
        APP_ENV=test php bin/console cache:clear
        APP_ENV=test php bin/console doctrine:migrations:migrate --no-interaction

    - name: Run PHPStan
      working-directory: backend
      run: |
        mkdir -p var/cache/phpstan
        composer lint

    - name: Run PHP CS Fixer (check only)
      working-directory: backend
      run: composer cs-check

    - name: Run PHPUnit
      working-directory: backend
      run: composer test

    - name: Run PHPUnit with coverage
      working-directory: backend
      if: matrix.php-version == '8.2'
      run: |
        mkdir -p var/coverage
        composer test-coverage || echo "Coverage generation failed, continuing without coverage"
      continue-on-error: true

  security-check:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.2
        tools: composer:v2

    - name: Create minimal .env file for Symfony
      working-directory: backend
      run: |
        echo "APP_ENV=prod" > .env
        echo "APP_SECRET=dev_secret_for_build_only" >> .env
        echo "DATABASE_URL=sqlite:///%kernel.project_dir%/var/data/database.sqlite" >> .env

    - name: Install dependencies
      working-directory: backend
      run: composer install --no-dev --no-progress --prefer-dist --optimize-autoloader

    - name: Security check
      working-directory: backend
      run: composer security-check

  build-docker:
    runs-on: ubuntu-latest
    needs: [test, security-check]
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Create minimal .env file for Docker build
      working-directory: backend
      run: |
        echo "APP_ENV=prod" > .env
        echo "APP_SECRET=docker_build_secret" >> .env
        echo "DATABASE_URL=sqlite:///%kernel.project_dir%/var/data/database.sqlite" >> .env

    - name: Build Docker image
      working-directory: backend
      run: |
        docker build --target runtime -t specsrv-backend:test .

    - name: Test Docker image
      working-directory: backend
      run: |
        docker run --rm specsrv-backend:test php bin/console --version